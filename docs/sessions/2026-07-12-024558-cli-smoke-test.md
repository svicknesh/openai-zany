# Session: Expand the read-only CLI smoke test

Date: 2026-07-12T02:45:58Z

## Changes made

- Added `zany sessions-json --limit 1` to the documented read-only smoke test.
- Added `zany docs-diff` so the smoke test also detects generated-document drift without modifying files.
- Clarified the expected human-readable, machine-readable, and freshness-check results.

## Validation

- Reviewed the complete updated `README.md` through the GitHub connector.
- Confirmed both commands are already documented primary CLI commands.
- No application behavior or managed generated document changed, so no test or regeneration change was required.

## Notes

- Work stayed inside `svicknesh/openai-zany`.
- `docs/session-log.md` was retrieved completely but left unchanged because repository instructions now designate it as a legacy archive for connector-driven work.
- Next smallest useful task: update the roadmap entry so only the blocked `session-log-check` smoke-test addition remains.